// Import jest globals
const { describe, it, expect } = require('@jest/globals');

// Skip all the tests in this file
describe.skip('Backend Optimization Tests', () => {
  it('should skip all tests', () => {
    expect(true).toBe(true);
  });
}); 